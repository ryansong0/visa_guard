public class Compound {
      public int getWeight(String comp, String atomics) {
        int sum = 0;
        int cat = 0;
        int val = 0;
        int alpha = 0;
          for (int i = 0; i < comp.length(); i++) {
                if (Character.isDigit("" + comp.charAt(i))) {
                    int cat = Integer.parseInt(comp.charAt(i))
                }
                if (Character.isAlphabetic(comp.charAt(i))) {
                    int alpha = atomic.indexOf(ch + ":");
                    if (Character.isDigit(alpha)) {
                        int val = Integer.parseInt(alpha)
                    }  
                }
                sum += val * cat;
            }
          }

          comp
          return 0;
      }
  }